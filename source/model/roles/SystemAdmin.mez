@Role("System Admin")
persistent object SystemAdmin {

   @requiredFieldValidator("validator.required_field")
   string firstName;
   string lastName;
   string mobileNumber;
   string emailAddress;

   bool deleted;
}